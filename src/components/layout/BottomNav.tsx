function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >@function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >@function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >@function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >@function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >3function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >jfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >3function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >3function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >6function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >&function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >&function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >3function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >!function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >&function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >&function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >&function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >&function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >4function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >4function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >jfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >+function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >jfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >3function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >3function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >6function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >jfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >4function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >_function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >_function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >_function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >4function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >9function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >7function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >4function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >3function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >8function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >8function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >?function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >:function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >zfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >5function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >8function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >8function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >wfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >jfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >'function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >kfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >{function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >.function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >[function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >(function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ffunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >,function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >0function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >xfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >]function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >cfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >=function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >bfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ufunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >4function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >pfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >rfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >sfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >yfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >-function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >1function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >2function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >"function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >`function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >hfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >mfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >Tfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ofunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >gfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >lfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >efunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >`function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >dfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >ifunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >`function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    ><function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >/function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >nfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >afunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >vfunction TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >>function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    > function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >)function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >;function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >}function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >
function TabButton({ tab, isActive, onClick }: { tab: TabConfig; isActive: boolean; onClick: () => void }) {
  const { Icon, label, badge } = tab;
  return (
    <button
      onClick={(e) => {
        e.stopPropagation();
        console.log("Tab clicked:", label);
        onClick();
      }}
      aria-label={label}
      aria-current={isActive ? "page" : undefined}
      className={cn("relative flex flex-col items-center justify-center gap-[3px] px-3 py-2 min-w-[56px] transition-colors duration-150 cursor-pointer z-50",
        isActive ? "text-gold" : "text-muted-foreground"
      )}
      style={{ touchAction: "manipulation" }}
    >