/** Stub — native push omitted so EAS free-tier build finishes under 45m */
export function usePushNotifications(_userId?: string | number | null) {
  return { expoPushToken: null as string | null, notification: null };
}
